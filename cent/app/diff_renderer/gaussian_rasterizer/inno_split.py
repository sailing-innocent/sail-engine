from module.utils.camera.basic import Camera 

import torch 

class GaussianRenderer:
    def __init__(self, config):
        # self.projector
        # self.sampler 
        pass 

    def render_scene(self, scene, cam):

        # gaussians_2d = self.projector(gaussians_3d, cam)
        # images = self.sampler(gaussians_2d, cam)
        pass 